using UnityEngine;
using System.Collections;
using System.Collections.Generic;

namespace HVRPuzzle
{
    public class PuzzleFactory : Singleton<PuzzleFactory>
    {
       void Start()
       {
            // TODO mikes
            // create a puzzle tool to save puzzle data (.asset)
            // load the .asset here
            // check saved data to see what puzzles were available (and active) for the player
            // instantiate puzzle related objects for active puzzles
       }
    }
}
